- name: Copy docker-compose for Rabbit-MQ
  copy:
    src: ./stuff/ci/docker-compose.yml
    dest: /root/docker-compose.yml
    mode: u=rw,g=rw,o=r #664
    owner: "{{ user_root }}"
    group: "{{ user_root }}"