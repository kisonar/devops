- name: Install Ansible module for Docker-Compose
  command: ansible-galaxy collection install community.general
  ignore_errors: yes

#covers
# community.general
# google.cloud
# ansible.posix
# ansible.netcommon
# community.kubernetes