# Ansible

## Usage
```

ansible-playbook -i hosts.ini playbook-installation.yml
ansible-playbook -i hosts.ini playbook-services.yml
ansible-playbook -i hosts.ini playbook-installation-localhost.yml
```